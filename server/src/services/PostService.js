import db from "../db/models/index.js";

export default class MeetingService {
  async createPost(groupId, meetingId, post) {}

  async readPost(postId) {}

  async updatePost(postId, post) {}

  async deletePost(postId) {}
}
