import React from "react";
import { Text, View } from "react-native";
import { MainWrapper } from "../../../components/common/MainWrapper";

const MapScreen = () => {
  return (
    <MainWrapper>
      <Text style={{ color: "#fff", fontSize: 40 }}>Map</Text>
    </MainWrapper>
  );
};

export default MapScreen;
