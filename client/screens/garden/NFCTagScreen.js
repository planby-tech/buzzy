import { Text } from "react-native";
import { MainWrapper } from "../../components/common/MainWrapper";

const NFCTagScreen = () => {
  return (
    <MainWrapper>
      <Text style={{ color: "#fff" }}>NFC Screen</Text>
    </MainWrapper>
  );
};

export default NFCTagScreen;
