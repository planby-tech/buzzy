import React from "react";
import { Text } from "react-native";
import { MainWrapper } from "../../components/common/MainWrapper";

const NewsScreen = () => {
  return (
    <MainWrapper>
      <Text style={{ color: "white", fontSize: 30 }}>News Page</Text>
    </MainWrapper>
  );
};

export default NewsScreen;
